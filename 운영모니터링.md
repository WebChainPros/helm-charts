# 🔍 FireFly 멀티체인 풀스택 트랜잭션, 논스(Nonce) & 병목 운영 모니터링 가이드 (`운영모니터링.md`)

본 문서는 **Hyperledger FireFly 풀스택 환경**에서 대량 트랜잭션 전송 시 펜딩(Pending) 트랜잭션 추적, 블록체인 계정 논스(Nonce) 동기화, EVMConnect Policy Loop 처리속도, PostgreSQL DB 병목 및 Grafana 메트릭 모니터링을 위한 핵심 명령어 집합 가이드입니다.

---

## ⏳ 1. 펜딩(Pending) 트랜잭션 & Policy Loop 처리 모니터링

FireFly Transaction Manager(EVMConnect)가 큐(Queue)에서 트랜잭션을 꺼내어 이더리움 노드로 전송하고 마이닝 영수증(Receipt)을 수신 중인지 확인하는 명령어입니다.

### 1.1 EVMConnect Policy Loop 처리 상태 실시간 추적
```bash
# Meta-net EVMConnect Policy Loop 실시간 모니터링
kubectl logs -f deploy/firefly-evmconnect -n firefly | grep -i policyloop

# Polygon Amoy 커넥터 Policy Loop 모니터링
kubectl logs -f deploy/evmconnect-amoy -n firefly | grep -i policyloop

# Ethereum Sepolia 커넥터 Policy Loop 모니터링
kubectl logs -f deploy/evmconnect-sepolia -n firefly | grep -i policyloop
```

### 1.2 펜딩 트랜잭션 해시(Hash) 및 처리 현황 추출
```bash
# 최근 500개 로그 중 펜딩(Pending) 상태의 트랜잭션 목록만 필터링
kubectl logs deploy/firefly-evmconnect -n firefly --tail 500 | grep -i "status=Pending"
```

---

## 🔢 2. 논스(Nonce) 추적 및 서명기(Web3Signer) 상태 모니터링

### 2.1 RPC 노드 블록 높이(Block Height) & Nonce 동기화 확인
EVMConnect가 이더리움 노드(`http://host.docker.internal:8545`)에서 최신 블록 및 계정 Nonce를 정상 조회 중인지 확인합니다:
```bash
# RPC 블록 수신 및 Nonce / BlockListener 상태 확인
kubectl logs deploy/firefly-evmconnect -n firefly --tail 200 | grep -i -E "blocknumber|nonce|blocklistener"
```

### 2.2 Web3Signer 트랜잭션 서명 처리 로그 확인
서명기가 K8s Secret 키스토어로 트랜잭션 암호화 서명을 정상 수행하는지 모니터링합니다:
```bash
# Meta-net Web3Signer 서명 로그
kubectl logs -f deploy/firefly-evmconnect-web3signer -n firefly

# Amoy / Sepolia Web3Signer 서명 로그
kubectl logs -f deploy/web3signer-amoy -n firefly
kubectl logs -f deploy/web3signer-sepolia -n firefly
```

---

## 🐢 3. 시스템 병목(Bottleneck) 및 성능 분석

### 3.1 PostgreSQL DB 트랜잭션 & 이벤트 배치 락 확인
FireFly Core Engine(`firefly-0`)이 DB에 이벤트를 락 없이 고속으로 기록하고 있는지 확인합니다:
```bash
# DB 쿼리 지연 및 Event Batching 처리 속도 모니터링
kubectl logs -f firefly-0 -n firefly | grep -i -E "SQL|batch|txwriter"
```

### 3.2 FireFly REST API 응답 속도 및 에러(4xx, 5xx) 추적
```bash
# REST API 지연 및 에러 응답 실시간 감시
kubectl logs -f firefly-0 -n firefly | grep -i -E "HTTP|POST|GET|412|500"
```

---

## 📊 4. REST API 기반 트랜잭션 & 네임스페이스 헬스체크 (CLI)

### 4.1 네임스페이스 준비 및 헬스 상태 조회
```bash
# 현재 FireFly Core 헬스 및 네임스페이스 활성화 상태 확인
curl -s http://127.0.0.1:5000/api/v1/status | jq .
```

### 4.2 특정 트랜잭션 ID 처리 상세 조회 (Receipt / Block Hash)
```bash
# 특정 트랜잭션 ID의 상세 상태 및 블록 수신 여부 확인
curl -s http://127.0.0.1:5000/api/v1/namespaces/meta/transactions/<TRANSACTION_ID> | jq .
```

---

## 📈 5. Grafana & Prometheus 실시간 GUI 모니터링

포트 포워딩 실행 후 브라우저 접속:
```bash
# 백그라운드 포트 포워딩 실행
kubectl port-forward svc/grafana 3300:3000 -n firefly --address 0.0.0.0 &
kubectl port-forward svc/prometheus 9090:9090 -n firefly --address 0.0.0.0 &
```

| 대시보드 | URL 주소 | 계정 / 설명 |
| :--- | :--- | :--- |
| **Grafana 그래픽 대시보드** | `http://127.0.0.1:3300` | ID: `admin` / PW: `admin` (TPS, DB Query, Latency) |
| **Prometheus 타겟 대시보드** | `http://127.0.0.1:9090` | 메트릭 타겟 수집 현황 확인 |

### 🎯 주요 관찰 메트릭
- **`firefly_tps`**: 초당 처리 트랜잭션 수 (Transactions Per Second)
- **`postgres_connections`**: DB 커넥션 풀 상태 (최대 400 튜닝)
- **`evmconnect_policyloop_pending_buffer`**: EVMConnect 펜딩 버퍼 큐 크기

---

## 🛠️ 6. 트러블슈팅 & 장애 원인 분석 명령어 집합

### 6.1 포트 포워딩 연결 끊김 & 포트 선점(Conflict) 확인
파드 재시작 후 웹 브라우저에서 `연결 거부(Connection Refused)` 또는 `bind: address already in use` 에러 발생 시 진단:

```bash
# 1. UI 및 API 웹 포트(5000) 응답 상태 헤더 점검 (HTTP 200 OK 여부)
curl -I http://127.0.0.1:5000/ui/

# 2. 현재 실행 중인 kubectl port-forward 백그라운드 프로세스 조회
ps aux | grep port-forward

# 3. 특정 포트(5000, 3300 등)를 선점하고 있는 로컬 프로세스 및 도커 컨테이너 확인
ss -tulpn | grep 5000
docker ps | grep 3000

# 4. 꼬인 포트 포워딩 프로세스 일괄 종료 후 백그라운드 재연결
pkill -f "port-forward"
kubectl port-forward svc/firefly-ui-proxy 5000:5000 -n firefly --address 0.0.0.0 &
kubectl port-forward svc/grafana 3300:3000 -n firefly --address 0.0.0.0 &
```

### 6.2 네임스페이스 초기화 멈춤 (`HTTP 412: Namespace is initializing`) 원인 분석
UI 접속 시 빈 화면이 나타나거나 `Namespace 'meta' is initializing` 에러 응답 시 진단:

```bash
# 1. 8545 블록체인 노드(RPC) 연결 거부 에러 감시 (connection refused)
kubectl logs deploy/firefly-evmconnect -n firefly --tail 50 | grep -i "connection refused"

# 2. 멀티체인 커넥터 타임아웃 에러 추적 (context deadline exceeded)
kubectl logs firefly-0 -n firefly --tail 100 | grep -i "context deadline exceeded"
```

### 6.3 커스텀 UI (`BlockchainSync` 대시보드) 빌드 & K8s 매핑 재적용
K8s 재배포 후 첫 화면의 노드별 논스/블록 추적 컴포넌트가 기본 오픈소스 UI로 리셋되었을 때 복구:

```bash
# 1. 로컬 ui/build 폴더를 커스텀 UI 도커 이미지로 빌드
cd /home/joon/firefly/ui
docker build -t firefly-custom-ui:latest -f - . <<'EOF'
FROM nginx:alpine
RUN rm -rf /usr/share/nginx/html/*
COPY build /usr/share/nginx/html
COPY build /usr/share/nginx/html/ui
EOF

# 2. UI 프록시 파드 재적용 및 순차 재시작
cd /home/joon/firefly/helm-charts
kubectl apply -f manifests/ui-proxy.yaml
kubectl rollout restart deploy/firefly-ui-proxy -n firefly
```

### 6.4 펜딩(Pending) 트랜잭션의 실제 수신 여부 vs 잔여 데이터 판별법 (`update=0`)

UI의 Operations 메뉴에서 트랜잭션이 지속적으로 `Pending`으로 표시될 때, 실제로 블록체인에서 영수증을 처리 중인지 아니면 과거 DB 잔여 기록인지 판별하는 명령어입니다:

```bash
# EVMConnect Policy Loop 로그 실시간 상태 검사
kubectl logs firefly-evmconnect-0 -n firefly --tail 50 | grep -i "Policy engine executed"
```

* **로그 해석 기준**:
  - **`update=0`**: 블록체인 노드에 해당 트랜잭션이 존재하지 않거나 영수증이 없어 아무런 상태 변경도 일어나지 않는 단순 대기 상태 (과거 노드 리셋 후 DB 잔여 기록)
  - **`update=1` / `status=Succeeded`**: 블록체인 노드에서 영수증(Receipt)을 정상 수신하여 펜딩 상태가 해제되고 있는 실제 진행 상태
