# 🚀 FireFly Custom UI 소스 수정 및 쿠버네티스 운영 반영 절차서 (운영반영절차.md)

본 문서는 **Hyperledger FireFly 커스텀 UI(React Web App)의 소스코드를 수정하고, 프로덕션 빌드, 도커 컨테이너화, 쿠버네티스(Kind/containerd) 이미지 이관 및 파드 롤아웃 반영까지의 전 과정을 정리한 운영 반영 절차서**입니다.

---

## 📋 개요 및 전체 반영 흐름도

```
[1. React UI 소스 수정] ➔ [2. .dockerignore 설정] ➔ [3. Docker 이미지 멀티스테이지 빌드]
                                                                  │
[6. 브라우저 캐시 초기화/검증] ◄─ [5. 파드 롤아웃 리스타트] ◄─ [4. containerd 이미지 이관]
```

---

## 🛠️ 1단계: React 소스코드 수정 (UI Component Modification)

FireFly Explorer의 메인 대시보드 화면에 원하는 컴포넌트 및 로직을 추가/수정합니다.

* **수정 대상 파일**: `ui/src/pages/Home/views/BlockchainSync.tsx`
* **주요 작업 내용**:
  1. `MinedTransfersState` 인터페이스 및 React State 선언
  2. FireFly Core API `/api/v1/namespaces/meta/tokens/transfers` 및 `/operations` 3초 주기 자동 페칭 로직 구현
  3. 상단 요약 카드 그리드(`Grid`) 및 Local Besu(`meta`) 카드 내 **On-Chain Mined Transfers** 카운터 UI 렌더링

---

## ⚡ 2단계: 빌드 속도 최적화 (`.dockerignore` 작성)

도커 빌드 시 무거운 `node_modules` 및 `build` 폴더가 도커 데몬으로 전송되는 병목 현상을 방지하기 위해 `.dockerignore` 파일을 생성합니다. (컨텍스트 전송 시간: 수십 초 ➔ **0.1초로 단축**)

* **파일 경로**: `ui/.dockerignore`

```dockerignore
node_modules
build
.git
.cache
.npm
```

---

## 📦 3단계: Docker 이미지 프로덕션 멀티스테이지 빌드

React 소스를 Node 18 환경에서 컴파일 및 번들링(`npm run build`)하고, Nginx 이미지로 경량화하여 도커 이미지를 생성합니다.

> 💡 **주의**: 코드 스타일(Prettier/ESLint) 차이로 인한 CI 컴파일 실패를 방지하기 위해 `ENV DISABLE_ESLINT_PLUGIN=true` 환경 변수를 포함합니다.

### 실행 명령어 (WSL 터미널):
```bash
cd /home/joon/firefly/ui

# 도커 멀티스테이지 빌드 및 최신 v4 태그 지정
docker build -t firefly-custom-ui:v4 -f - . << 'EOF'
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install --legacy-peer-deps
COPY . .
ENV DISABLE_ESLINT_PLUGIN=true
RUN npm run build

FROM nginx:alpine
RUN rm -rf /usr/share/nginx/html/*
COPY --from=builder /app/build /usr/share/nginx/html
COPY --from=builder /app/build /usr/share/nginx/html/ui
EOF
```

---

## 🔄 4단계: 쿠버네티스 노드(containerd)로 도커 이미지 이관

로컬 Docker 데몬에 빌드된 이미지를 쿠버네티스 컨트롤 플레인 노드(`desktop-control-plane`)의 `containerd` 네임스페이스(`k8s.io`)로 직접 주입합니다.

### 실행 명령어:
```bash
# Docker 이미지를 tar 스트림으로 내보낸 후 containerd로 수동 이관
docker save firefly-custom-ui:v4 | docker exec -i desktop-control-plane ctr -n k8s.io images import -
```

---

## ♻️ 5단계: 파드 롤아웃 리스타트 (Pod Rollout Restart)

쿠버네티스 `firefly-ui-proxy` 배포(Deployment)를 롤링 리스타트하여 새 이미지를 파드에 적용합니다.

### 실행 명령어:
```bash
# 1. UI Proxy 파드 롤링 리스타트
kubectl rollout restart deployment/firefly-ui-proxy -n firefly

# 2. 파드 재시작 상태 및 러닝 여부 확인
kubectl get pods -l app=firefly-ui-proxy -n firefly
```

---

## 🌐 6단계: 포트 포워딩 및 서비스 검증

로컬 PC(브라우저)에서 쿠버네티스 5000번 포트로 접속할 수 있도록 포트 포워딩을 유지하고 접속을 검증합니다.

### 1) 백그라운드 포트 포워딩 실행
```bash
nohup kubectl port-forward svc/firefly-ui-proxy 5000:5000 -n firefly --address 0.0.0.0 > /dev/null 2>&1 &
```

### 2) 서비스 정상 응답 및 정적 파일 번들 검증
```bash
# HTML 응답 확인
curl -s http://127.0.0.1:5000/ui/ | grep "main"

# 최신 번들 내 수정한 키워드 포함 여부 확인
curl -s http://127.0.0.1:5000/ui/static/js/main.*.js | grep -i -o "Mined Transfers"
```

### 3) 웹 브라우저 접속 및 캐시 초기화
* **웹 브라우저 접속 주소**: [http://127.0.0.1:5000/ui/](http://127.0.0.1:5000/ui/)
* **브라우저 캐시 초기화 (필수)**: `Ctrl` + `Shift` + `R` (또는 `Ctrl` + `F5`)

---

## 📑 Quick Reference (한 줄 실행 스크립트)

UI 소스 수정 후 한 번에 전체 빌드 및 반영을 수행하려면 아래 원라인 명령어를 사용할 수 있습니다:

```bash
cd /home/joon/firefly/ui && \
docker build -t firefly-custom-ui:v4 -f - . << 'EOF'
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install --legacy-peer-deps
COPY . .
ENV DISABLE_ESLINT_PLUGIN=true
RUN npm run build
FROM nginx:alpine
RUN rm -rf /usr/share/nginx/html/*
COPY --from=builder /app/build /usr/share/nginx/html
COPY --from=builder /app/build /usr/share/nginx/html/ui
EOF
docker save firefly-custom-ui:v4 | docker exec -i desktop-control-plane ctr -n k8s.io images import - && \
kubectl rollout restart deployment/firefly-ui-proxy -n firefly
```
