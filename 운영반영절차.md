# 🚀 FireFly Custom UI 소스 수정 및 쿠버네티스 운영 반영 절차서 (운영반영절차.md)

본 문서는 **Hyperledger FireFly 커스텀 UI(React Web App)의 소스코드를 수정하고, 프로덕션 빌드, GHCR(GitHub Container Registry) 푸시, 쿠버네티스 배포 및 5500번 표준 포트 포워딩까지의 전 과정을 정리한 운영 반영 절차서**입니다.

---

## 📌 핵심 운영 원칙 요약

1. **일상 배포 시 (Helm Chart 배포)**:
   - 이미 원격 레지스트리(`ghcr.io/webchainpro-cpu/firefly-custom-ui:v9`)에 이미지가 배포되어 있으므로, **별도의 소스코드 클론이나 빌드 없이 `./deploy.sh` 및 `manifests/` 적용만으로 즉시 배포**됩니다.
2. **UI 기능 개발 / 수정 시**:
   - UI 소스코드가 변경되었을 때만 아래 **1~5단계** 절차를 거쳐 새 버전을 빌드하고 GHCR로 푸시합니다.
3. **표준 접속 포트 (`5500번`) 지정**:
   - macOS 환경에서는 OS 기본 기능인 **AirPlay 수신 모드(AirTunes/ControlCenter)가 5000번 포트를 점유**하고 있어 `403 Forbidden` 충돌이 발생합니다.
   - 따라서 프론트엔드 UI 및 통합 프록시 접속은 **`5500번 포트`(`5500:5000`)를 표준 포트로 사용**합니다.

---

## 📋 전체 반영 흐름도

```text
[1. React UI 소스 수정] ➔ [2. .dockerignore 최적화] ➔ [3. Docker 멀티스테이지 빌드]
                                                                  │
[6. 5500번 포트 포워딩 검증] ◄─ [5. 파드 롤아웃 리스타트] ◄─ [4. GHCR 이미지 푸시]
```

---

## 🛠️ 1단계: React 소스코드 수정 (UI Component Modification)

FireFly Explorer의 메인 대시보드 화면에 원하는 컴포넌트 및 로직을 추가/수정합니다.

* **수정 대상 파일**: `ui/src/pages/Home/views/BlockchainSync.tsx`
* **주요 작업 내용**:
  1. `MinedTransfersState` 인터페이스 및 React State 선언
  2. FireFly Core API `/api/v1/namespaces/meta/tokens/transfers` 및 `/operations` 3초 주기 자동 페칭 로직 구현
  3. 상단 요약 카드 그리드(`Grid`) 및 Local Besu(`meta`) 카드 내 **On-Chain Mined Transfers** 카운터 UI 렌더링

---

## ⚡ 2단계: 빌드 속도 최적화 (`.dockerignore` 작성)

도커 빌드 시 무거운 `node_modules` 및 `build` 폴더가 도커 데몬으로 전송되는 병목 현상을 방지하기 위해 `.dockerignore` 파일을 유지합니다. (컨텍스트 전송 시간: 수십 초 ➔ **0.1초로 단축**)

* **파일 경로**: `ui/.dockerignore`

```dockerignore
node_modules
build
.git
.cache
.npm
```

---

## 📦 3단계: Docker 이미지 프로덕션 멀티스테이지 빌드

React 소스를 Node 18 환경에서 컴파일 및 번들링(`npm run build`)하고, Nginx 이미지로 경량화하여 도커 이미지를 생성합니다.

> 💡 **주의**: 코드 스타일(Prettier/ESLint) 차이로 인한 CI 컴파일 실패를 방지하기 위해 `ENV DISABLE_ESLINT_PLUGIN=true` 환경 변수를 포함합니다.

### 실행 명령어:
```bash
# UI 소스 디렉토리 이동
cd /Users/joon/workspaces/firefly/ui

# 도커 멀티스테이지 빌드 및 GHCR 태그 부여
docker build -t ghcr.io/webchainpro-cpu/firefly-custom-ui:v9 -f - . << 'EOF'
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install --legacy-peer-deps
COPY . .
ENV DISABLE_ESLINT_PLUGIN=true
RUN npm run build

FROM nginx:alpine
RUN rm -rf /usr/share/nginx/html/*
COPY --from=builder /app/build /usr/share/nginx/html
COPY --from=builder /app/build /usr/share/nginx/html/ui
EOF
```

---

## ☁️ 4단계: GHCR(GitHub Container Registry) 원격 저장소 푸시

빌드된 이미지를 GHCR로 푸시하여, 다른 환경이나 신규 배포 시 소스코드 빌드 없이 쿠버네티스가 자동으로 이미지를 다운로드받을 수 있게 합니다.

### 실행 명령어:
```bash
# GHCR 로그인 (최초 1회 또는 토큰 만료 시)
# echo $CR_PAT | docker login ghcr.io -u <깃허브계정명> --password-stdin

# 이미지 푸시
docker push ghcr.io/webchainpro-cpu/firefly-custom-ui:v9
```

---

## ♻️ 5단계: 파드 롤아웃 리스타트 (Pod Rollout Restart)

쿠버네티스 `firefly-ui-proxy` 배포(Deployment)를 롤링 리스타트하여 새 이미지를 파드에 적용합니다.

### 실행 명령어:
```bash
cd /Users/joon/workspaces/helm-charts

# 1. UI Proxy 파드 롤링 리스타트
kubectl rollout restart deployment/firefly-ui-proxy -n firefly

# 2. 파드 재시작 상태 및 러닝 여부 확인
kubectl rollout status deployment/firefly-ui-proxy -n firefly
```

---

## 🌐 6단계: 5500번 포트 포워딩 및 서비스 검증 (중요 ⭐)

macOS의 기본 **AirPlay 수신 모드(AirTunes)**가 5000번 포트를 점유하므로, **`5500`번 포트로 포트 포워딩을 실행**합니다.

### 1) 백그라운드 포트 포워딩 실행
```bash
nohup kubectl port-forward svc/firefly-ui-proxy 5500:5000 -n firefly --address 0.0.0.0 > /dev/null 2>&1 &
```

### 2) 서비스 정상 응답 및 정적 파일 번들 검증
```bash
# HTML 응답 확인 (200 OK & Title 확인)
curl -s http://127.0.0.1:5500/ui/ | grep "FireFly Explorer"

# 최신 번들 내 수정한 컴포넌트 키워드 포함 여부 확인
curl -s http://127.0.0.1:5500/ui/static/js/main.*.js | grep -i -o "Mined Transfers"
```

### 3) 웹 브라우저 접속 및 캐시 초기화
* **웹 브라우저 접속 주소**: [http://127.0.0.1:5500/ui/](http://127.0.0.1:5500/ui/)
* **Core REST API 주소**: [http://127.0.0.1:5500/api/v1/status](http://127.0.0.1:5500/api/v1/status)
* **브라우저 캐시 초기화 (필수)**: `Cmd` + `Shift` + `R` (또는 `Ctrl` + `Shift` + `R`)

---

## 📑 Quick Reference (원스톱 실행 스크립트)

UI 소스 수정 후 한 번에 빌드, 푸시 및 K8s 파드 갱신을 수행할 때:

```bash
# 1. 소스 빌드 & GHCR 푸시
cd /Users/joon/workspaces/firefly/ui && \
docker build -t ghcr.io/webchainpro-cpu/firefly-custom-ui:v9 -f - . << 'EOF'
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install --legacy-peer-deps
COPY . .
ENV DISABLE_ESLINT_PLUGIN=true
RUN npm run build
FROM nginx:alpine
RUN rm -rf /usr/share/nginx/html/*
COPY --from=builder /app/build /usr/share/nginx/html
COPY --from=builder /app/build /usr/share/nginx/html/ui
EOF
docker push ghcr.io/webchainpro-cpu/firefly-custom-ui:v9

# 2. 파드 재시작 & 5500 포트 포워딩
cd /Users/joon/workspaces/helm-charts && \
kubectl rollout restart deployment/firefly-ui-proxy -n firefly && \
nohup kubectl port-forward svc/firefly-ui-proxy 5500:5000 -n firefly --address 0.0.0.0 > /dev/null 2>&1 &
```
